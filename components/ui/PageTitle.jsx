export default function PageTitle({ title }) {
    return (
        <div className="pb-6 border-b-6 border-gray-300">
            <h1 className="text-3xl font-bold">{title}</h1>
        </div>
    );
}